package edu.vanderbilt.cs.live9.ast.interpreter;

public enum ExpressionType {
    FIND,
    GREATER_THAN,
    LESS_THAN,
    ATTRIBUTE,
    AND,
    WHERE,
    NEAR,
    DOUBLE,
    INTEGER,
    INVALID_EXPRESSION;
}
